// Packages
import React from 'react';

// Components
import { Layout, Login, Data } from '../components';

const LoginPage = () => (
  <Layout>
    <Login />
    <Data />
  </Layout>
);

export default LoginPage;
