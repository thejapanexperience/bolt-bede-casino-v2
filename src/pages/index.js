// Packages
import React from 'react';

// Components
import { Layout, Modals, GamesList } from '../components';

const IndexPage = () => (
  <Layout>
    <Modals />
    <GamesList />
  </Layout>
);

export default IndexPage;
